name: CI
on:
  push:
    branches: [ main ]

  pull_request:

permissions:
  contents: write

jobs:
  native-image:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ 'ubuntu-latest', 'macos-latest' ]
         include:
           - os: macOS-latest
             uploaded_filename: cmta-x86_64-apple-darwin
             local_path: cmta/target/native-image/cmta
           - os: ubuntu-latest
             uploaded_filename: cmta-x86_64-pc-linux
             local_path: cmta/target/native-image/cmta
        #   - os: windows-latest
        #     uploaded_filename: cmta-x86_64-pc-win32.exe
        #     local_path: cmta\target\native-image\cmta

    env:
      # define Java options for both official sbt and sbt-extras
      JAVA_OPTS: -Xms2048M -Xmx2048M -Xss6M -XX:ReservedCodeCacheSize=256M -Dfile.encoding=UTF-8
      JVM_OPTS:  -Xms2048M -Xmx2048M -Xss6M -XX:ReservedCodeCacheSize=256M -Dfile.encoding=UTF-8

    steps:
      - name: Native image generation
        shell: bash
        run: |
          echo $(matrix.os) $(matrix.uploaded_filename) $(matrix.local_path)

